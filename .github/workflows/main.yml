name: Scheduling Actions using schedule event types and CRON expressions

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the current time
        run: echo 'The current server time is $(date)'
